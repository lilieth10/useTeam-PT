import { KanbanBoard } from '@/components/kanban/KanbanBoard';

/**
 * Página principal del tablero Kanban
 * Ahora es un componente simple que delega toda la lógica al KanbanBoard
 */
const Index = () => {
  return <KanbanBoard />;
};

export default Index;
